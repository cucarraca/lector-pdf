name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'
    
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.27.1'
        channel: 'stable'
    
    - name: Get dependencies
      run: flutter pub get
    
    - name: Run tests
      run: flutter test --no-pub
      continue-on-error: true
    
    - name: Build APK
      run: flutter build apk --release
    
    - name: Upload APK to artifacts
      uses: actions/upload-artifact@v4
      with:
        name: release-apk
        path: build/app/outputs/flutter-apk/app-release.apk
        retention-days: 30
    
    - name: Rename APK with version
      run: |
        VERSION="v23-separadores-pagina"
        BUILD_NUM="${{ github.run_number }}"
        mv build/app/outputs/flutter-apk/app-release.apk build/app/outputs/flutter-apk/lector-pdf-${VERSION}-build${BUILD_NUM}.apk
    
    - name: Upload APK to artifacts
      uses: actions/upload-artifact@v4
      with:
        name: lector-pdf-v23-separadores-pagina
        path: build/app/outputs/flutter-apk/lector-pdf-*.apk
        retention-days: 30
    
    - name: Create Release
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v23-build${{ github.run_number }}
        name: Lector PDF v23 - Separadores de Página - Build ${{ github.run_number }}
        body: |
          ## 📱 Lector PDF - Versión con Separadores v23
          
          ### ✅ Mejoras Implementadas:
          - ✨ **Navegación con indicador arrastrable (slider vertical)**
          - � **Separadores visuales entre páginas**
          - ✅ Todo el texto cargado de una vez (caché instantáneo)
          - ✅ Marcadores "Página X" entre secciones del texto
          - ✅ Scroll fluido por todo el documento
          - ✅ Vista PDF completamente limpia
          
          ### 🎯 Cambios en esta versión:
          - Texto extraído completo con separadores visuales "──── Página X ────"
          - Carga única al abrir (máxima rapidez)
          - Scroll continuo por todo el documento
          - Indicador arrastrable funciona como slider de navegación
          - Caché se mantiene durante toda la sesión
          
          ### 🎮 Controles:
          - **Tap en indicador:** Abrir diálogo "Go to page"
          - **Arrastrar indicador:** Navegar entre páginas (slider vertical)
          - **Scroll normal:** Desplazarse por el texto completo
          - **Controles inferiores:** Play, pause, navegación
          
          ### 📦 Instalación:
          1. Descarga el APK
          2. Permite instalación de fuentes desconocidas
          3. Instala la app
          
          **Compilado:** Octubre 2025
        files: build/app/outputs/flutter-apk/lector-pdf-*.apk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
